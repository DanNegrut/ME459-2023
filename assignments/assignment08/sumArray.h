#ifndef SUMARRAY_H
#define SUMARRAY_H
#include <stdlib.h>

// Each function should calculate the sum of all entries in 
// matrices A, which is an n by n matrix represented as a 1D array
double sumArray1(const double* A, const size_t n);
double sumArray2(const double* A, const size_t n);

#endif
